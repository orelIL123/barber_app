# פתרון בעיות - קביעת תורים

## בעיה: משתמש לא יכול לקבוע תור

אם יש משתמש שלא יכול לקבוע תור ומופיעה השגיאה "לא ניתן לקבוע את התור", יש כמה דברים לבדוק:

### 1. בדיקת משתמשים כפולים

הריצו את הסקריפט הבא כדי לבדוק אם יש כמה משתמשים עם אותו מספר טלפון:

```bash
node scripts/checkUserBooking.js 0559274580
```

הסקריפט יחפש:
- משתמשים עם אותו מספר טלפון בפורמטים שונים
- האם המשתמש קיים ב-Firestore
- האם המשתמש קיים ב-Firebase Authentication
- כמה תורים יש למשתמש
- האם יש Push Token

### 2. בעיות אפשריות ופתרונות

#### בעיה: יש כמה משתמשים עם אותו מספר טלפון
**פתרון:**
1. הזהה את המשתמש הנכון (לפי תאריך יצירה, או לפי המשתמש שהמשתמש משתמש בו)
2. מחק את המשתמשים הכפולים מהמערכת
3. ודא שהמשתמש מתחבר עם המשתמש הנכון

#### בעיה: המשתמש לא קיים ב-Firestore
**פתרון:**
1. ודא שהמשתמש נרשם בהצלחה
2. אם המשתמש קיים ב-Auth אבל לא ב-Firestore, צריך ליצור פרופיל למשתמש
3. אפשר לנסות להכנס ולצאת שוב מהמערכת

#### בעיה: המשתמש לא קיים ב-Firebase Authentication
**פתרון:**
1. המשתמש צריך להיכנס שוב למערכת
2. אם זה לא עוזר, צריך למחוק את המשתמש מה-Auth וליצור אותו מחדש

### 3. שימוש בפונקציה checkDuplicateUsersByPhone

בקוד, ניתן להשתמש בפונקציה `checkDuplicateUsersByPhone` כדי לבדוק אם יש משתמשים כפולים:

```typescript
import { checkDuplicateUsersByPhone } from './services/firebase';

const duplicates = await checkDuplicateUsersByPhone('0559274580');
if (duplicates.length > 1) {
  console.warn(`Found ${duplicates.length} duplicate users!`);
  // Handle duplicates
}
```

### 4. לוגים

הקוד עכשיו כולל לוגים מפורטים יותר. כאשר משתמש מנסה לקבוע תור, תראו בקונסול:
- האם המשתמש קיים ב-Firestore
- שם המשתמש ומספר הטלפון
- פרטי השגיאה אם יש

### 5. בדיקת הודעות שגיאה

ההודעות שגיאה עכשיו יותר ספציפיות:
- אם המשתמש לא נמצא: "שגיאה: המשתמש לא נמצא במערכת. נא להתחבר מחדש או ליצור קשר עם התמיכה."
- אם יש שגיאה אחרת: ההודעה תציג את פרטי השגיאה

## הרצת הסקריפט

```bash
cd /path/to/barber_app
node scripts/checkUserBooking.js 0559274580
```

החלף את `0559274580` במספר הטלפון של המשתמש שיש לו בעיה.


